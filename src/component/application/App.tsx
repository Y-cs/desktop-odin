import React, { Component } from 'react';

export default class App extends Component {
  constructor(props: any) {
    super(props);
    this.state={
      title:props.title,
    }
  }

  render() {
    return (
      <div>

      </div>
    );
  }
}
